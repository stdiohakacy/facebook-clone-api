export enum ENUM_NOTIFICATION_STATUS {
    UNREAD = 'unread',
    READ = 'read',
}

export enum ENUM_NOTIFICATION_PROGRESS {
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
    FAILED = 'failed',
}

export enum ENUM_NOTIFICATION_TYPE {
    TOPIC = 'topic',
    MULTICAST = 'multicast',
    SINGLE = 'single',
}
