export enum ENUM_MESSAGE_STATUS {
    CREATED = 'created',
    PENDING = 'pending',
    SEEN = 'seen',
}

export enum ENUM_MESSAGE_EVENT_KEY {
    MESSAGE_CREATE = 'message.create',
    MESSAGE_CREATED = 'message.created',
}
